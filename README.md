# rudydahbura.auth0.com

## Usage

Example usage of the Auth0 Deploy CLI utility:

```json
{
  "AUTH0_DOMAIN": "...",
  "AUTH0_CLIENT_ID": "...",
  "AUTH0_CLIENT_SECRET": "..."
}
```

```bash
a0deploy export -c config.json -f directory -o ./
```
